# ===================================================
# Social Stax - Environment Variables Template
# ===================================================
# IMPORTANT: This is a TEMPLATE file with placeholder values
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control
# See README.md for detailed setup instructions
# ===================================================

# ===================================================
# FIREBASE CONFIGURATION (Required)
# ===================================================
# Get these values from Firebase Console:
# 1. Go to https://console.firebase.google.com/
# 2. Select your project
# 3. Click Settings (gear icon) > Project Settings
# 4. Scroll to "Your apps" section
# 5. Click the web app and select "Config" tab
# 6. Copy the values below

VITE_FIREBASE_API_KEY=YOUR_ACTUAL_API_KEY_FROM_FIREBASE_CONSOLE
VITE_FIREBASE_AUTH_DOMAIN=YOUR_PROJECT_ID.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=YOUR_PROJECT_ID
VITE_FIREBASE_STORAGE_BUCKET=YOUR_PROJECT_ID.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=YOUR_MESSAGING_SENDER_ID
VITE_FIREBASE_APP_ID=YOUR_APP_ID
VITE_FIREBASE_URL=https://YOUR_PROJECT_ID.firebaseio.com
VITE_GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_FROM_CLOUD_CONSOLE

# ===================================================
# GOOGLE GEMINI API (Optional - for AI features)
# ===================================================
# Get from: https://console.cloud.google.com/apis/credentials
# Make sure Generative Language API is enabled
# Can be same as VITE_GOOGLE_API_KEY or different

# VITE_GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_FROM_CLOUD_CONSOLE

# ===================================================
# ADMIN CONFIGURATION (Development Only)
# ===================================================
# These are default admin credentials for local development
# NEVER use these in production
# NEVER commit real credentials to this file
# Use GitHub Secrets for production deployments

VITE_ADMIN_EMAIL=admin@example.com
VITE_ADMIN_PASSWORD=SecurePassword123!
VITE_ADMIN_SECRET_KEY=your_development_secret_key_change_for_production

# ===================================================
# STRIPE CONFIGURATION (Optional - for payments)
# ===================================================
# Get from: https://dashboard.stripe.com/apikeys
# Only needed if using Stripe for payments

# VITE_STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key_here

# ===================================================
# SETUP INSTRUCTIONS
# ===================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Open .env.local in your editor
# 3. Replace all placeholder values with your actual credentials
# 4. Save the file
# 5. Run: npm run dev
# 6. Your app will now have access to Firebase

# ===================================================
# SECURITY NOTES
# ===================================================
# - Never commit .env.local to version control
# - .env.local is included in .gitignore
# - In production, set environment variables via:
#   * GitHub Secrets (for GitHub Actions)
#   * Vercel/Netlify environment settings (for hosting)
#   * Cloud Provider secrets manager (AWS Secrets Manager, etc.)
# - Rotate your API keys if they are accidentally exposed
# - Use different keys for development and production
